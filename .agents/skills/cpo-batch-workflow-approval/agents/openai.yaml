interface:
  display_name: "Batch Approval Review Assistant"
  short_description: "Review the user's pending approvals in a batch, flag compliance risks, and process safely after confirmation"
  default_prompt: "Use $cpo-batch-workflow-approval to review all my pending approvals, list risks and items that can be approved directly, then process them in a batch after I confirm."
